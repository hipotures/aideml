# path to the task data directory
data_dir: null

# either provide a path to a plaintext file describing the task
desc_file: null
# or provide the task goal (and optionally evaluation information) as arguments
goal: null
eval: null

log_dir: logs
workspace_dir: workspaces

# whether to unzip any archives in the data directory
preprocess_data: True
# whether to copy the data to the workspace directory (otherwise it will be symlinked)
# copying is recommended to prevent the agent from accidentally modifying the original data
copy_data: True

exp_name: null # a random experiment name will be generated if not provided

# settings for code execution
exec:
  timeout: 3600
  agent_file_name: runfile.py
  format_tb_ipython: False
  memory_limit_gb: 80

generate_report: True
# LLM settings for final report from journal
report:
  model: gpt-5.4-mini
  temp: 1.0
  reasoning_effort: low

# optional external Codex research advisor
research:
  enabled: False
  mode: llm
  every_steps: 10
  top_k_best: 5
  top_k_worst: 5
  previous_summary_count: 5
  timeout: 900
  model: gpt-5.4-mini
  reasoning_effort: low
  manual_sample_size: 3
  manual_seed: 42
  hypothesis_root_limit: 100
  # default: stable randomized root ordering with usage de-duplication.
  # manifest_score: prioritize roots by the best numeric score recorded in
  # code_manifest.json for hypothesis_root_score_mode.
  hypothesis_root_order: default
  hypothesis_root_score_mode: autogluon
  # Number of concurrent hypothesis ROOT code generations in --generate-only.
  # Valid range is 1..8. Default 1 preserves serial behavior.
  hypothesis_root_generate_workers: 1
  # If true, a new hypothesis run starts with already executed/scored ROOT code
  # from code_manifest.json materialized as completed tree nodes. Ignored on
  # --resume. Default false preserves empty-run behavior.
  seed_scored_roots: False
  # If true, manual hypothesis selection treats every enabled hypothesis as
  # compatible with the current agent mode. Useful for deriving AutoGluon
  # experiments from legacy-only hypotheses.
  ignore_hypothesis_agent_modes: False

# optional external Codex solution synthesis
synthesis:
  enabled: False
  every_scored_steps: 15
  top_k: 5
  # current = use only this run; all = scan sibling log runs. Explicit
  # source_runs below overrides this scope.
  source_scope: current
  source_runs: []
  score_round_decimals: 5
  prediction_round_decimals: 5
  prediction_similarity_sample_size: 200
  prediction_similarity_min_common_sample_size: 100
  prediction_similarity_rmse_threshold: 0.015
  timeout: 900
  model: gpt-5.4-mini
  reasoning_effort: low

# agent hyperparams
agent:
  # how many improvement iterations to run
  steps: 20
  # code generation mode: legacy writes full ML scripts; autogluon_preprocess
  # asks the LLM for feature preprocessing only and uses a fixed AutoGluon runner
  mode: legacy
  # whether to instruct the agent to use CV (set to 1 to disable)
  k_fold_validation: 5
  # whether to instruct the agent to generate a prediction function
  expose_prediction: False
  # whether to provide the agent with a preview of the data
  data_preview: True
  # whether legacy full-script prompts should prefer CUDA/GPU-capable tabular models
  gpu: false
  # auxiliary data mode:
  # false/off = normal competition files
  # true/merged/merge = use prebuilt train-aux/test-aux files as train/test
  # <file.csv|file.csv.gz> = expose one raw auxiliary CSV flat in workspace/input
  aux: false

  # LLM settings for coding
  code:
    model: gpt-5.4-mini
    temp: 0.5
    reasoning_effort: low

  # LLM settings for evaluating program output / tracebacks
  feedback:
    model: gpt-5.4-mini
    temp: 0.5
    reasoning_effort: low

  # hyperparameters for the tree search
  search:
    max_debug_depth: 3
    debug_prob: 0.5
    num_drafts: 5
    exploration_weight: 0.0
    best_score_min_children_before_exploration: 5
    disable_oom_saturated_parents: false
    hypothesis_child_order: root_score
    # Hypothesis mode only: when set to a root hypothesis id, search only expands
    # that root subtree while keeping global best-score reporting intact.
    forced_root: null
    # Hypothesis mode only: when set to a hypothesis id, run/debug only that
    # exact hypothesis and do not create child-hypothesis branches.
    forced_hypothesis: null
    hypothesis_max_non_improving_children_per_parent: 10
    hypothesis_min_improvement_epsilon: 0.00006

  autogluon:
    # Named profiles for the fixed AutoGluon runner.
    profile: s6e6_boost_gpu
    # Dedicated timeout for generated preprocess(df), separate from AutoGluon fit time_limit.
    preprocess_timeout: 180
    profiles:
      fast_boost:
        included_model_types: [XGB, GBM]
        presets: medium_quality
        time_limit: 600
        use_gpu: false
        validation_strategy: holdout
        fit_args:
          save_space: true
          fit_weighted_ensemble: false
          auto_stack: false
      full_boost:
        included_model_types: [XGB, GBM, CAT]
        presets: medium_quality
        time_limit: 600
        use_gpu: false
        validation_strategy: holdout
        fit_args:
          save_space: true
          fit_weighted_ensemble: false
          auto_stack: false
      s6e6_boost:
        included_model_types: [XGB, GBM, CAT]
        presets: medium_quality
        time_limit: 600
        use_gpu: false
        validation_strategy: holdout
        fit_args:
          save_space: true
          fit_weighted_ensemble: false
          auto_stack: false
      s6e6_boost_ens:
        included_model_types: [XGB, GBM, CAT]
        presets: medium_quality
        time_limit: 600
        use_gpu: false
        validation_strategy: holdout
        class_balance: balanced
        fit_args:
          save_space: true
          fit_weighted_ensemble: true
          auto_stack: false
      full_boost_gpu:
        included_model_types: [XGB, GBM, CAT]
        presets: medium_quality
        time_limit: 600
        use_gpu: true
        validation_strategy: holdout
        hyperparameters:
          XGB:
            - device: cuda
              tree_method: hist
              ag_args:
                priority: 999
              ag_args_fit:
                num_gpus: 1
          GBM:
            - device: cuda
              ag_args_fit:
                num_gpus: 1
          CAT:
            - task_type: GPU
              devices: "0"
              gpu_ram_part: 0.8
              ag_args_fit:
                num_gpus: 1
        fit_args:
          save_space: true
          fit_weighted_ensemble: false
          auto_stack: false
      s6e6_boost_gpu:
        included_model_types: [XGB, GBM, CAT]
        presets: medium_quality
        time_limit: 600
        use_gpu: true
        validation_strategy: holdout
        hyperparameters:
          XGB:
            - device: cuda
              tree_method: hist
              ag_args:
                priority: 999
              ag_args_fit:
                num_gpus: 1
          GBM:
            - device: cuda
              ag_args_fit:
                num_gpus: 1
          CAT:
            - task_type: GPU
              devices: "0"
              gpu_ram_part: 0.8
              ag_args_fit:
                num_gpus: 1
        fit_args:
          save_space: true
          fit_weighted_ensemble: false
          auto_stack: false
      s6e6_boost_gpu_ens:
        included_model_types: [XGB, GBM, CAT]
        presets: medium_quality
        time_limit: 600
        use_gpu: true
        validation_strategy: holdout
        class_balance: balanced
        hyperparameters:
          XGB:
            - device: cuda
              tree_method: hist
              ag_args:
                priority: 999
              ag_args_fit:
                num_gpus: 1
          GBM:
            - device: cuda
              ag_args_fit:
                num_gpus: 1
          CAT:
            - task_type: GPU
              devices: "0"
              gpu_ram_part: 0.8
              ag_args_fit:
                num_gpus: 1
        fit_args:
          save_space: true
          fit_weighted_ensemble: true
          auto_stack: false
      full_boost_gpu_ens:
        included_model_types: [XGB, GBM, CAT]
        presets: medium_quality
        time_limit: 600
        use_gpu: true
        validation_strategy: holdout
        hyperparameters:
          XGB:
            - device: cuda
              tree_method: hist
              ag_args:
                priority: 999
              ag_args_fit:
                num_gpus: 1
          GBM:
            - device: cuda
              ag_args_fit:
                num_gpus: 1
          CAT:
            - task_type: GPU
              devices: "0"
              gpu_ram_part: 0.8
              ag_args_fit:
                num_gpus: 1
        fit_args:
          save_space: true
          fit_weighted_ensemble: true
          auto_stack: false
      best_boost_gpu_30m:
        included_model_types: [XGB, GBM, CAT]
        presets: best
        time_limit: 1800
        use_gpu: true
        hyperparameters:
          XGB:
            - device: cuda
              tree_method: hist
              ag_args:
                priority: 999
              ag_args_fit:
                num_gpus: 1
          GBM:
            - device: cuda
              ag_args_fit:
                num_gpus: 1
          CAT:
            - task_type: GPU
              devices: "0"
              gpu_ram_part: 0.8
              ag_args_fit:
                num_gpus: 1
        fit_args:
          save_space: true

      best_boost_gpu_1h:
        included_model_types: [XGB, GBM, CAT]
        presets: best
        time_limit: 3600
        preprocess_timeout: 180
        validation_fraction: 0.2
        validation_strategy: autogluon
        seed: 42
        class_balance: balanced
        use_gpu: true
        hyperparameters:
          XGB:
            - device: cuda
              tree_method: hist
              ag_args:
                priority: 999
              ag_args_fit:
                num_gpus: 1
          GBM:
            - device: cuda
              ag_args_fit:
                num_gpus: 1
          CAT:
            - task_type: GPU
              devices: "0"
              gpu_ram_part: 0.8
              ag_args_fit:
                num_gpus: 1
        fit_args: {}
      best_xgb_gpu_1h:
        included_model_types: [XGB]
        presets: best
        time_limit: 3600
        preprocess_timeout: 180
        validation_fraction: 0.2
        validation_strategy: autogluon
        seed: 42
        class_balance: balanced
        use_gpu: true
        hyperparameters:
          XGB:
            - device: cuda
              tree_method: hist
              ag_args:
                priority: 999
              ag_args_fit:
                num_gpus: 1
        fit_args: {}
      best_xgb_cpu_1h:
        included_model_types: [XGB]
        presets: best
        time_limit: 3600
        preprocess_timeout: 180
        validation_fraction: 0.2
        validation_strategy: autogluon
        seed: 42
        class_balance: balanced
        use_gpu: false
        hyperparameters:
          XGB:
            - device: cpu
              tree_method: hist
              ag_args:
                priority: 999
              ag_args_fit:
                num_gpus: 0
        fit_args: {}
      best_boost_1h:
        included_model_types: [XGB, GBM, CAT]
        presets: best
        time_limit: 3600
        use_gpu: false
        fit_args:
          save_space: true
      best_boost_2h:
        included_model_types: [XGB, GBM, CAT]
        presets: best
        time_limit: 7200
        use_gpu: false

      full_best_30m:
        included_model_types: [XGB, GBM, CAT]
        presets: best_quality
        time_limit: 1800
      full_best_30m_gpu:
        included_model_types: [XGB, GBM, CAT]
        presets: best_quality
        time_limit: 1800
        use_gpu: true
        hyperparameters:
          XGB:
            - device: cuda
              tree_method: hist
              ag_args:
                priority: 999
              ag_args_fit:
                num_gpus: 1
          GBM:
            - device: cuda
              ag_args_fit:
                num_gpus: 1
          CAT:
            - task_type: GPU
              devices: "0"
              gpu_ram_part: 0.8
              ag_args_fit:
                num_gpus: 1
    # Fallback values used only if a profile does not define a field.
    presets: medium_quality
    time_limit: 600
    validation_fraction: 0.2
    seed: 42
    included_model_types: null
    validation_strategy: null
    use_gpu: null
    hyperparameters: null
    fit_args: null
